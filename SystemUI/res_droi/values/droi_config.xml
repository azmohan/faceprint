<?xml version="1.0" encoding="utf-8"?>
<!--
/*
** Copyright 2009, The Android Open Source Project
**
** Licensed under the Apache License, Version 2.0 (the "License");
** you may not use this file except in compliance with the License.
** You may obtain a copy of the License at
**
**     http://www.apache.org/licenses/LICENSE-2.0
**
** Unless required by applicable law or agreed to in writing, software
** distributed under the License is distributed on an "AS IS" BASIS,
** WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
** See the License for the specific language governing permissions and
** limitations under the License.
*/
-->

<!-- These resources are around just to allow their values to be customized
     for different hardware and product builds. -->
<resources>

    <!-- Whether we're using the tablet-optimized recents interface (we use this
     value at runtime for some things) -->
    <bool name="droi_config_recents_interface_for_tablets">false</bool>

    <!-- Whether recents thumbnails should stretch in both x and y to fill their
     ImageView -->
    <bool name="droi_config_recents_thumbnail_image_fits_to_xy">false</bool>

    <!-- Whether recents should use hardware layers for its taskviews. This flag can be enabled
    for devices where the java drawing of round rects may be slow -->
    <bool name="droi_config_recents_use_hardware_layers">false</bool>

    <!-- The number of app thumbnails we keep in memory -->
    <integer name="droi_config_recents_max_thumbnail_count">10</integer>

    <!-- The number of app icons we keep in memory -->
    <integer name="droi_config_recents_max_icon_count">20</integer>

    <!-- Whether to use cheap, less good looking shadows for recents -->
    <bool name="droi_config_recents_fake_shadows">false</bool>

    <!-- Whether we're using the tablet-optimized recents interface (we use this
     value at runtime for some things) -->
    <integer name="droi_status_bar_recents_bg_gradient_degrees">90</integer>

    <!-- The duration in seconds to wait before the dismiss buttons are shown. -->
    <integer name="droi_recents_task_bar_dismiss_delay_seconds">1</integer>

    <!-- The min animation duration for animating views that are currently visible. -->
    <integer name="droi_recents_filter_animate_current_views_duration">250</integer>

    <!-- The min animation duration for animating views that are newly visible. -->
    <integer name="droi_recents_filter_animate_new_views_duration">250</integer>

    <!-- The duration of the window transition when coming to Recents from an app.
         In order to defer the in-app animations until after the transition is complete,
         we also need to use this value as the starting delay when animating the first
         task decorations in. -->
    <integer name="droi_recents_enter_from_app_transition_duration">325</integer>

    <!-- The duration for animating the task decorations in after transitioning from an app. -->
    <integer name="droi_recents_task_enter_from_app_duration">200</integer>

    <!-- The duration for animating the task decorations out before transitioning to an app. -->
    <integer name="droi_recents_task_exit_to_app_duration">125</integer>

    <!-- The duration of the window transition when coming to Recents from the Launcher.
         In order to defer the in-app animations until after the transition is complete,
         we also need to use this value as the starting delay when animating the task views
         in from the bottom of the screen. -->
    <integer name="droi_recents_enter_from_home_transition_duration">100</integer>

    <!-- The duration for animating the task from the bottom of the screen when transitioning
     from home. -->
    <integer name="droi_recents_task_enter_from_home_duration">225</integer>

    <!-- The stagger for each task when animating the task from the bottom of the screen when
     transitioning from home. -->
    <integer name="droi_recents_task_enter_from_home_stagger_delay">12</integer>

    <!-- The duration of the animation of the tasks to the bottom of the screen when leaving
     Recents to go back to the Launcher. -->
    <integer name="droi_recents_task_exit_to_home_duration">225</integer>

    <!-- The min animation duration for animating the nav bar scrim in. -->
    <integer name="droi_recents_nav_bar_scrim_enter_duration">400</integer>

    <!-- The animation duration for animating the removal of a task view. -->
    <integer name="droi_recents_animate_task_view_remove_duration">250</integer>

    <!-- The animation duration for scrolling the stack to a particular item. -->
    <integer name="droi_recents_animate_task_stack_scroll_duration">225</integer>

    <!-- The minimum alpha for the dim applied to cards that go deeper into the stack. -->
    <integer name="droi_recents_max_task_stack_view_dim">96</integer>

    <!-- The delay to enforce between each alt-tab key press. -->
    <integer name="droi_recents_alt_tab_key_delay">200</integer>

    <!-- Transposes the search bar layout in landscape. -->
    <bool name="droi_recents_has_transposed_search_bar">true</bool>

    <!-- Transposes the nav bar in landscape (only used for purposes of layout). -->
    <bool name="droi_recents_has_transposed_nav_bar">true</bool>
    <!-- Svelte specific logic, see RecentsConfiguration.SVELTE_* constants. -->
    <integer name="droi_recents_svelte_level">0</integer>
</resources>

